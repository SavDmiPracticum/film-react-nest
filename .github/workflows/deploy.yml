name: Build and Push Docker Images to GHCR

on:
  push:
    branches: [ review-3 ]

env:
  REGISTRY: ghcr.io
  IMAGE_BACKEND: ghcr.io/${{ github.repository_owner }}/film-backend
  IMAGE_FRONTEND: ghcr.io/${{ github.repository_owner }}/film-frontend
  IMAGE_NGINX: ghcr.io/${{ github.repository_owner }}/film-nginx

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner | toLower }}/film-backend:latest
            ghcr.io/${{ github.repository_owner | toLower }}/film-backend:${{ github.sha }}

      - name: Build and push frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner | toLower }}/film-frontend:latest
            ghcr.io/${{ github.repository_owner | toLower }}/film-frontend:${{ github.sha }}

      - name: Build and push nginx
        uses: docker/build-push-action@v5
        with:
          context: ./nginx
          file: ./nginx/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner | toLower }}/film-nginx:latest
            ghcr.io/${{ github.repository_owner | toLower }}/film-nginx:${{ github.sha }}
